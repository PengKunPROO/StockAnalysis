# 选股器 v2 重构 - 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 重构选股器：因子从 6 个扩展到 ~14 个可用 + 4 个标记不可用；6 个策略模板；因子说明 tooltip；数据校验展开行；Pass2 上限提升到 50。跳过东财 push2 估值因子和自定义派生因子。

**Architecture:** 扩展现有 FilterField + ScreenerEngine。新增技术因子（K线计算）、消息面因子（龙虎榜/热榜/资金流）、因子 description、数据校验。前端 ScreenerView 增加因子说明 tooltip + 结果展开校验行。

**Tech Stack:** FastAPI + Pydantic v2 (后端); React 19 + TypeScript (前端); 同花顺 API + 东财 eastmoney.py (数据源)

## Global Constraints

- 后端端口 8002，股票代码格式 sh.600519 / sz.000001
- 所有路由 async def，Pydantic v2 模型
- 东财 push2 stock/get 不可达 -> PE/PB/总市值/换手率标记 available=false
- 同花顺可用：snapshot(change_pct/amount/amplitude/open/high/low), financials(roe/debt_ratio), dragon-tiger, hot-stock
- eastmoney.py 可用：fetch_north_bound, fetch_sector_rank
- 数据源失败优雅降级，返回空 + warning，不 500
- 测试用 TestClient（module-scoped）+ pytest_asyncio
- 工作目录：D:\AI\hermesStockAgent\.worktrees\screener
- Backend venv: D:/AI/hermesStockAgent/backend/.venv

---

## File Structure

```
backend/app/screener/
├── fields.py          # MODIFY: 扩展 FilterField (description/source/pass_phase), 新增14+因子
├── engine.py          # MODIFY: 扩展 Pass2 (财务+技术+消息面), 上限50, 6策略, 数据校验
└── __init__.py        # 不变

backend/app/api/v1/
└── screener.py        # MODIFY: run 端点返回因子明细

backend/tests/
└── test_screener.py   # CREATE: 因子+引擎+策略测试

frontend/src/
├── components/
│   └── ScreenerView.tsx  # MODIFY: 因子tooltip + 结果展开校验行 + 新因子UI
└── api/
    └── screener.ts       # 可能需要小改（如果有新字段）
```

---

### Task 1: 扩展 FilterField 模型 + 因子定义 + 测试

**Files:**
- Modify: `backend/app/screener/fields.py`
- Test: `backend/tests/test_screener.py`

**Interfaces:**
- Produces: 扩展后的 `FilterField` (含 description, source, pass_phase 字段)
- Produces: `FIELDS` 列表 (~18 个因子，14 可用 + 4 不可用)
- Produces: `SNAPSHOT_FIELDS_NAMES`, `PASS2_FIELD_NAMES`, `TECH_FIELD_NAMES`, `NEWS_FIELD_NAMES`, `SORTABLE`

- [ ] **Step 1: Write failing tests**

```python
# backend/tests/test_screener.py
import pytest
from app.screener.fields import get_fields, matches_snapshot, FIELD_BY_NAME, FIELDS


class TestFields:
    def test_field_count(self):
        fl = get_fields()
        assert len(fl) >= 18  # 14 available + 4 unavailable

    def test_all_fields_have_description(self):
        for f in get_fields():
            assert f.description, f"Field {f.name} missing description"

    def test_all_fields_have_source(self):
        for f in get_fields():
            assert f.source, f"Field {f.name} missing source"

    def test_all_fields_have_pass_phase(self):
        for f in get_fields():
            assert f.pass_phase in (1, 2), f"Field {f.name} invalid pass_phase"

    def test_snapshot_fields_available(self):
        # These must be available (fuyao snapshot provides them)
        for name in ["change_pct", "amount", "amplitude", "open", "high", "low"]:
            f = FIELD_BY_NAME[name]
            assert f.available, f"{name} should be available"
            assert f.pass_phase == 1

    def test_valuation_fields_unavailable(self):
        # Eastmoney push2 unreachable -> mark unavailable
        for name in ["pe_ttm", "pb", "total_mv", "turnover_rate"]:
            f = FIELD_BY_NAME[name]
            assert not f.available, f"{name} should be unavailable"
            assert f.pass_phase == 2

    def test_financial_fields_available(self):
        for name in ["roe", "debt_ratio"]:
            f = FIELD_BY_NAME[name]
            assert f.available
            assert f.pass_phase == 2

    def test_tech_fields_available(self):
        for name in ["macd_golden_cross", "rsi_oversold", "boll_breakout", "kdj_golden_cross",
                      "near_high_drop", "change_5d", "change_20d"]:
            assert name in FIELD_BY_NAME, f"{name} not defined"
            assert FIELD_BY_NAME[name].available

    def test_news_fields_available(self):
        for name in ["on_dragon_tiger", "hot_rank", "main_net_flow"]:
            assert name in FIELD_BY_NAME, f"{name} not defined"
            assert FIELD_BY_NAME[name].available
            assert FIELD_BY_NAME[name].pass_phase == 2

    def test_matches_snapshot_change_pct(self):
        stock = {"change_pct": 5.5, "amount": 1000000, "amplitude": 3.2}
        assert matches_snapshot(stock, {"change_pct": {"min": 3}})
        assert not matches_snapshot(stock, {"change_pct": {"min": 6}})

    def test_matches_snapshot_amount(self):
        stock = {"change_pct": 1.0, "amount": 5000000}
        assert matches_snapshot(stock, {"amount": {"min": 1000000}})
        assert not matches_snapshot(stock, {"amount": {"min": 6000000}})

    def test_matches_snapshot_amplitude(self):
        stock = {"change_pct": 1.0, "amplitude": 6.5}
        assert matches_snapshot(stock, {"amplitude": {"min": 5}})
        assert not matches_snapshot(stock, {"amplitude": {"min": 7}})

    def test_matches_snapshot_no_filters(self):
        stock = {"change_pct": 1.0}
        assert matches_snapshot(stock, {})

    def test_sortable_includes_new_fields(self):
        from app.screener.fields import SORTABLE
        assert "change_pct" in SORTABLE
        assert "amount" in SORTABLE
        assert "amplitude" in SORTABLE
        assert "roe" in SORTABLE
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd D:/AI/hermesStockAgent/.worktrees/screener/backend && D:/AI/hermesStockAgent/backend/.venv/Scripts/python -m pytest tests/test_screener.py -v --tb=short`
Expected: FAIL (description/source/pass_phase not in FilterField)

- [ ] **Step 3: Rewrite fields.py with expanded fields**

在 `backend/app/screener/fields.py` 中：

1. 扩展 `FilterField` 模型，增加 `description: str = ""`, `source: str = ""`, `pass_phase: int = 1`
2. 重写 `FIELDS` 列表，包含所有 18 个因子（6 快照 + 4 估值不可用 + 2 财务 + 7 技术 + 3 消息面 = 22，减去重复 = ~18）
3. 更新 `SNAPSHOT_FIELDS`, `PASS2_FIELDS`, `TECH_FIELDS`, 新增 `NEWS_FIELDS`
4. 更新 `SORTABLE` 加入新可排序字段
5. `matches_snapshot` 逻辑不变（仍只过滤 SNAPSHOT_FIELDS 中的可用字段）

详细因子列表见 spec。

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd D:/AI/hermesStockAgent/.worktrees/screener/backend && D:/AI/hermesStockAgent/backend/.venv/Scripts/python -m pytest tests/test_screener.py -v --tb=short`
Expected: All PASS

- [ ] **Step 5: Commit**

```bash
cd D:/AI/hermesStockAgent/.worktrees/screener
git add backend/app/screener/fields.py backend/tests/test_screener.py
git commit -m "feat: expand screener fields to 18 factors with description/source/pass_phase + tests"
git push
```

---

### Task 2: 扩展 ScreenerEngine + 6 策略模板 + 数据校验 + 测试

**Files:**
- Modify: `backend/app/screener/engine.py`
- Modify: `backend/app/api/v1/screener.py`
- Test: `backend/tests/test_screener.py` (追加)

**Interfaces:**
- Consumes: Task 1 的扩展字段定义
- Produces: 6 个策略模板, Pass2 扩展（财务+技术+消息面）, 数据校验返回因子明细

- [ ] **Step 1: Write failing tests**

在 `tests/test_screener.py` 追加：

```python
from app.screener.engine import list_skills, run_screener, SKILLS
from unittest.mock import AsyncMock, patch, MagicMock


class TestSkills:
    def test_six_skills(self):
        skills = list_skills()
        assert len(skills) == 6

    def test_skill_names(self):
        skills = list_skills()
        names = [s["name"] for s in skills]
        assert "oversold_bounce" in names
        assert "value" in names
        assert "technical" in names
        assert "news_driven" in names
        assert "breakout" in names
        assert "cigarette_butt" in names or "butt" in names

    def test_skill_has_fields(self):
        for s in list_skills():
            assert "name" in s
            assert "label" in s
            assert "description" in s
            assert "fields" in s


class TestRunScreener:
    @pytest.mark.asyncio
    async def test_run_no_source(self):
        with patch("app.screener.engine.get_source_for_market", return_value=None):
            result = await run_screener({})
            assert result["results"] == []
            assert "warning" in result

    @pytest.mark.asyncio
    async def test_run_pass1_only(self):
        """Test that Pass1 snapshot filtering works without Pass2."""
        mock_source = MagicMock()
        mock_source.name = "tonghuashun"
        mock_source.fetch_market_snapshot = AsyncMock(return_value=[
            {"code": "sh.600519", "name": "贵州茅台", "change_pct": 5.5, "amount": 10000000, "amplitude": 3.2},
            {"code": "sz.000001", "name": "平安银行", "change_pct": -1.0, "amount": 5000000, "amplitude": 2.0},
        ])
        mock_source.fetch_ticker_list = AsyncMock(return_value=[])

        with patch("app.screener.engine.get_source_for_market", return_value=mock_source):
            result = await run_screener({"change_pct": {"min": 3}})
            assert result["count"] == 1
            assert result["results"][0]["code"] == "sh.600519"
            # Verify factor details are included
            assert "factor_details" in result["results"][0]

    @pytest.mark.asyncio
    async def test_run_pass2_limit_50(self):
        """Pass2 should handle up to 50 stocks (not 30)."""
        mock_stocks = [
            {"code": f"sh.60000{i}", "name": f"股票{i}", "change_pct": 5.0, "amount": 10000000}
            for i in range(60)
        ]
        mock_source = MagicMock()
        mock_source.name = "tonghuashun"
        mock_source.fetch_market_snapshot = AsyncMock(return_value=mock_stocks)
        mock_source.fetch_ticker_list = AsyncMock(return_value=[])

        with patch("app.screener.engine.get_source_for_market", return_value=mock_source):
            # With roe filter (Pass2), should process up to 50
            with patch("app.screener.engine._apply_pass2", AsyncMock(return_value=mock_stocks[:50])):
                result = await run_screener({"roe": {"min": 10}})
                # Verify _apply_pass2 was called with up to 50 stocks
                assert result["count"] <= 50
```

- [ ] **Step 2: Run tests to verify they fail**

- [ ] **Step 3: Rewrite engine.py**

1. 更新 `SKILLS` 为 6 个策略模板（按 spec 调整后的）
2. 更新 `_apply_pass2` 支持新因子：
   - 财务：roe, debt_ratio（同花顺 financials）
   - 技术：macd_golden_cross, rsi_oversold, boll_breakout, kdj_golden_cross, near_high_drop, change_5d, change_20d（K线计算）
   - 消息面：on_dragon_tiger, hot_rank, main_net_flow（同花顺 dragon-tiger/hot-stock + 东财资金流）
3. Pass2 上限从 30 改为 50
4. 每只幸存股票返回 `factor_details` dict：每个因子的实际值 vs 阈值，绿✓/红✗
5. 优雅降级：消息面数据源失败不影响其他因子

- [ ] **Step 4: Update screener.py API**

在 `run` 端点的 `RunRequest` 中增加 `sort` 的可选值（加入 near_high_drop, change_5d 等）。

- [ ] **Step 5: Run tests to verify they pass**

- [ ] **Step 6: Commit**

---

### Task 3: 前端 ScreenerView 更新 + 因子 tooltip + 数据校验展开行

**Files:**
- Modify: `frontend/src/components/ScreenerView.tsx`
- Modify: `frontend/src/index.css` (追加因子 tooltip + 校验行样式)
- Modify: `backend/app/api/v1/screener.py` (如果需要新字段)

**Interfaces:**
- Consumes: Task 1+2 的后端 API（fields 含 description, run 返回 factor_details）

- [ ] **Step 1: Update ScreenerView.tsx**

1. 每个因子 label 旁加 tooltip 图标（hover 显示 `field.description`）
2. 新增因子按 category 分组显示（行情表现/基本面/技术信号/消息面）
3. 不可用因子灰显（available=false 的显示但禁用输入）
4. 结果表格每行可展开，显示 `factor_details`：因子名 | 实际值 | 阈值 | ✓/✗
5. 策略模板从 4 个更新为 6 个

- [ ] **Step 2: Add CSS for tooltip + expandable rows**

追加 CSS：
- `.field-tooltip` 因子说明 tooltip 样式
- `.field-unavailable` 灰显样式
- `.factor-detail-row` 展开行样式
- `.factor-pass` / `.factor-fail` 绿✓/红✗ 标记

- [ ] **Step 3: Run tsc + build**

Run: `cd D:/AI/hermesStockAgent/frontend && npx tsc --noEmit --project D:/AI/hermesStockAgent/.worktrees/screener/frontend/tsconfig.json`
Run: `npx vite build --config D:/AI/hermesStockAgent/.worktrees/screener/frontend/vite.config.ts --outDir /tmp/screener-build`

- [ ] **Step 4: Run verify.sh**

- [ ] **Step 5: Commit**

---

## Self-Review

### Spec coverage
| Spec 需求 | Task |
|-----------|------|
| 14+ 可用因子 | Task 1 |
| 4 不可用因子标记 | Task 1 |
| 因子 description tooltip | Task 1 (后端) + Task 3 (前端) |
| 6 策略模板 | Task 2 |
| Pass2 上限 50 | Task 2 |
| 数据校验展开行 | Task 2 (后端 factor_details) + Task 3 (前端) |
| 消息面因子 | Task 2 |
| 技术因子扩展 (kdj/52周/5日/20日) | Task 2 |
| Pass1 快照因子扩展 (open/high/low) | Task 1 |
| 跳过东财 push2 估值因子 | Task 1 (available=false) |
| 跳过自定义派生因子 | 不实现 |

### Placeholder scan: 无 TBD/TODO
### Type consistency: FilterField 扩展字段在 engine + API + 前端一致使用
